<%- include('../partials/header') %>
  <body>
    <h1>Skills</h1>
    <ul>
      <% skills.forEach(function(s) { %>
        <li>
          <a href="/skills/<%= s.id %>"><%= s.skill %></a>
            - 
          <%= s.proficient ? 'proficient' : 'not proficient' %>
        </li>
      <% }); %>
    </ul>
  </body>
  <%- include('../partials/footer') %>
