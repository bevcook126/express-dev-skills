<%- include('../partials/header') %>
  <body>
    <h1>Skills</h1>
    <ul>
      <% skills.forEach(function(t) { %>
        <li>
          <a href="/skills/<%= t.id %>"><%= t.skill %></a>
            - 
          <%= t.proficient ? 'proficient' : 'not proficient' %>
        </li>
      <% }); %>
    </ul>
  </body>
  <%- include('../partials/footer') %>
